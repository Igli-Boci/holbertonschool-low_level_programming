#include "main.h"

/**
 * _strstr - Function that find a substring
 * @haystack: Pointer
 * @needle: Substring
 * Return: needle or '\0'
 */

char *_strstr(char *haystack, char *needle)
{
	int l = 0;
	int k = 0;

	while (haystack[l] != '\0')
	{
		l = haystack;
		k = need
		while (needle[k] != '\0')
		{
			k++;
		}
		l++;
	}
	return ('\0');
}
