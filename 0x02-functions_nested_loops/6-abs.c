#include "main.h"
/**
 * _abs - check for the value of the integer
 * @n: The absolute value of an integer
 * Return: 0
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
